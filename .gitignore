node_modules/
package-lock.json
package.json

# Local Whisper / CT2 model caches (large binaries — downloaded on demand)
models/
__pycache__/
*.pyc
.DS_Store

# Source PDFs — too large for git. Rasterized images live in freewings_images/.
freewings/*.pdf

# Personal Claude Code overrides
.claude/settings.local.json
.vercel

# Podcast TTS — secrets + render scratch/cache (never commit keys or temp WAVs)
.env.local
scripts/.podcast_build/
